services:
  redis:
    image: redis
    ports:
      - 6379:6379

  nginx:
    image: nginx
    ports:
      - 8000:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    
  sever1:
    build: ./
    env_file:
      - ./.env
    
    ports:
      - 5001:5000


  
